import Constants from "expo-constants";

const { manifest } = Constants;

// const localUri = `http://${manifest.debuggerHost.split(":").shift()}:6009`;

export default {
  uri: "http://192.168.43.236:6001",
  // uri: localUri,
  // uri: "http://10.1.17.47:6009",
};
